<UserControl x:Class="BookStore.Presentation.View.AddOrUpdateAuthorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookStore.Presentation.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding AffectedAuthor}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
    <TextBlock Text="Update / Add author" 
               Foreground="White"
               FontSize="24"/>
        <StackPanel Grid.Row="1">
            
        <Label Content="First Name" Foreground="White" FontSize="18"/>
        <TextBox Text="{Binding FirstName}" Width="150"
                 HorizontalAlignment="Left"
                 Margin="5 0 0 0"/>
        </StackPanel>
        
    </Grid>
</UserControl>
